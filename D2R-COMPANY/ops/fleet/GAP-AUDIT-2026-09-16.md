# Offline twin gap audit — 2026-09-16

**Branch tip:** `sandbox/integration` @ `87aa3d3`  
**Worktree:** `/private/tmp/d2r-merch-integration`  
**Twin app:** `d2r-app/`  
**Sources:** fleet docs, `SITE-MAP.md`, domain `TWIN-STATUS.md`, `PARITY-CHECKLIST.md`, `GAPS-FILLED.md`, live capture report, `nav.ts`, `qc-twin-smoke.mjs`  
**Smoke (this pass):** **PASS** — 21/21 seeds, 23/23 required pages · `tsc` previously green on tip

---

## 1. Executive summary

Wave 3 integration delivered a **navigable offline audit skeleton**, not a full 1:1 production clone.

| Metric | Value |
|--------|-------|
| Discovered live routes (`URLS.txt`) | **67** |
| Twin `page.tsx` present | **47 / 67 ≈ 70%** |
| Intentional `PendingLane` stubs | **16 / 67 ≈ 24%** |
| Still missing pages | **3 / 67 ≈ 4%** (`/signup`, `/admin` redirect, `/test/dashboard`) — `/stores` + `/inventory/transfers/preview` filled |
| Navigable (page **or** pending chrome) | **62–63 / 67 ≈ 93–94%** |
| Smoke-required pages | **23 / 23** |
| Live-shaped seed depth | Strong on directory tables; **thin** on paginated ops + finance |

**Usable offline today (audit / demo):** DEV_AUTH login → admin dashboard, inventory levels (ALP×Adam Scott sample), ledgers/performance seeds, warehouses (60), brand-levels refresh table, transfers **page-1**, merchandising admin Q3 (41 reps) + period switcher, Shopify health (29) + brand stores, users (189), businesses (51), rep-assignments **page-1**, orders **page-1**, pulse signals (Adam Scott), payouts/commissions/settlements/report **scaffolds**, rep `/merchandising` offline rollup + optional iframe.

**Not cutover-ready:** full inventory matrix, full orders/transfers/assignments dumps, live finance truth, CRM cash-ops / accounts / contacts / locations, Shopify write path, production Clerk/Vercel, merch visit engine as first-class in-app routes (still CRA/iframe).

`PARITY-CHECKLIST.md`’s older “~57% (38/67)” figure is **stale** post–link-crawl + wave 3; use **~70% pages / ~94% navigable** for this tip.

---

## 2. Gaps by severity

### P0 — Blocks serious audit / cutover use

| ID | Gap | Why P0 |
|----|-----|--------|
| P0-DATA-PAGINATION | Orders (~1034 pages / ~20 rows seeded), transfers (~21 pages / 20 rows), assignments (~227 pages / 50 of **11,314** primary) | Ops audit cannot trust totals, search, or export from page-1 samples |
| P0-INV-MATRIX | Inventory seed is **one** brand×warehouse (27 SKUs ALP / Adam Scott) | Consignment audit across ~17 brands × 60 warehouses impossible offline |
| P0-FINANCE-LIVE | Live commissions capture **empty** (“Loading orders…”); twin finance rows are **scaffold**, not vault truth | Money path cannot be reconciled for cutover |
| P0-SHOPIFY-HEALTH | Live **9/29** shops healthy; twin mirrors the gap but cannot gate multi-shop sync with real tokens | Inventory/order truth depends on broken live scopes |
| P0-AUTH-DEPLOY | Company GitHub / Vercel / Clerk still owner-blocked; twin is DEV_AUTH + local JSON | No company-controlled staging URL for client audit |
| P0-NEXT-CVE | Locked `next@15.5.7` marked **deprecated** (security advisory 2025-12-11; patch line ≥15.5.9) | Must not ship staging on known-vulnerable Next |
| P0-ROLE-SPLIT | Admin/rep/customer surfaces not 1:1 — **rep nav expanded + offline scope landed** (see note); accounts/contacts still PendingLane; customer cluster incomplete | “All pages match” still blocked on CRM customers + remaining scope gaps (orders/assignments page-1) |

### Role-split parity (admin vs rep vs customers) — required for “all pages match”

Live D2R is **not admin-only**. Twin must mirror three surfaces:

| Surface | Live pattern | Twin today | Verdict |
|---------|--------------|------------|---------|
| **Admin** | `/admin/*` — cross-rep, all brands | Strongest (~70% real pages) | Primary build; still 16 PendingLanes |
| **Rep** | `/inventory`, `/orders`, `/merchandising`, `/retail-stores`, `/stores`, sales-status | **`REP_NAV` expanded** (inventory/ledgers/performance/transfers+create, orders/drafts, merch, retail-stores, stores, sales-status); `/stores` + transfer **preview** stubs; **session scope** via `rep-scope.ts` (demo Field Rep → Adam Scott) | **P0 improved (nav)** — still thin on orders/assignments page-1 match + column parity |
| **Customers / CRM** | Admin accounts, contacts, businesses + doors | Businesses OK; **accounts/contacts PendingLane**; retail-stores derived | **P0 for customer cluster** |

**Admin ↔ rep counterpart matrix (must match headers; different scope):**

| Module | Admin | Rep counterpart | Twin gap |
|--------|-------|-----------------|----------|
| Inventory levels | `/admin/inventory` | `/inventory` | Sample shown only when warehouse matches scoped rep (ALP×Adam Scott) |
| Ledgers / performance | `/admin/inventory/ledgers|performance` | `/inventory/ledgers|performance` | Filtered to matched rep via ledger seed |
| Transfers | `/admin/inventory/transfers` | `/inventory/transfers` (+ create/preview/receive) | List filtered by warehouse; create/preview stubs |
| Orders | `/admin/orders` (+ drafts) | `/orders` (+ drafts) | Filtered by Sales Rep — often empty on page-1 sample |
| Merchandising | `/admin/merchandising` | `/merchandising` | Already rep-scoped; scope banner added |
| Retail / doors | `/admin/retail-stores` | `/retail-stores` | Filtered from assignment-derived doors (page-1 may miss rep) |
| Brand stores | `/admin/stores` | `/stores` | **Rep `/stores` added** — ledger-brand filter when possible |
| Sales status | `/admin/sales-status` | `/locations/sales-status` | Admin pending; rep stub with matched-rep default |
| Pulse / payouts / reports / Shopify / users | Admin-only on live | — | OK if confirmed admin-only |

**P0-ROLE-SPLIT rep nav (2026-09-16 follow-up):** `REP_NAV` expanded to live tree; `/stores` + `/inventory/transfers/preview` added; `rep-scope.ts` scopes inventory/ledgers/performance/transfers/orders/retail-stores (demo Field Rep → Adam Scott, fuzzy banner). **Still global / unscoped:** sales-status table body (no seed), order/assignment page-1 misses for Adam Scott, merch Circle K routes already scoped separately, admin surfaces unchanged. Customers PendingLanes untouched.

**Next build wave:** (1) ~~expand `REP_NAV`~~ done, (2) deepen per-rep dumps so scoped lists are non-empty, (3) fill Customers PendingLanes (accounts/contacts), (4) column-diff admin vs rep per module against live CDP.

### P1 — Parity holes (pages exist or stubbed; fidelity incomplete)

| ID | Gap |
|----|-----|
| P1-PENDING-16 | 16 admin routes are title-only `PendingLane` (CRM/locations, merch stores, transfer terms, Shopify apps/test, payments/deposits/receipts, emails/files, order timeline) |
| P1-REP-TREE | Rep nav **expanded**; `/stores` + transfer preview added; offline session scope for inventory/transfers/orders/doors; remaining: empty page-1 matches for some reps, sales-status table seed, column parity vs live CDP |
| P1-MERCH-IFRAME | Visit engine still CRA + optional iframe (`NEXT_PUBLIC_MERCH_APP_URL`); dual auth + `frame-ancestors` / CSP risk; not in-app routes |
| P1-ORDERS-DEPTH | Drafts / detail / timeline / filters / export are stubs; create is demo-only |
| P1-TRANSFERS-UX | Approve/receive/detail partial; admin Actions mostly RO; terms pending |
| P1-PULSE | Goals / health / scores stub; comboboxes unwired; single-rep sample |
| P1-REPORTS | Hub + runners use ledger/commission **proxies**; no dedicated brand/rep sales captures |
| P1-RETAIL-STORES | Admin retail-stores **derived** from assignment sample (no dedicated seed) |
| P1-WEBHOOKS-RUNTIME | HMAC + routes exist; runtime ops JSONL / install logs / automations are local stubs; write path disabled; no multi-shop fan-out |

### P2 — Nice / live-system improvements (parked in backlog)

| ID | Gap |
|----|-----|
| P2-UI-PIXEL | No pixel-perfect live UI / authenticated CDP control inventory |
| P2-SIGNUP-TEST | `/signup`, `/test/dashboard` out of audit scope |
| P2-POSTGRES | File `store.ts` interim — Postgres swap documented only |
| P2-NAV-IA | Dense sidebar, Brands vs Platform stores duplication (live product) |
| P2-LIVE-OPS | Scope gate, pagination UX, role hygiene, unpriced SKU queue, webhook miss alerts — see `IMPROVEMENTS-BACKLOG.md` |

---

## 3. Route table

**Legend — twin status:** `done` usable table/shell · `partial` thin/stub UI · `pending` middleware PendingLane · `missing` no page  
**Legend — data status:** `live-full` · `live-sample` · `scaffold` · `derived` · `none`

| Route | Twin status | Data status | Notes |
|-------|-------------|-------------|-------|
| `/` | done | none | Role redirect |
| `/login` | done | none | DEV_AUTH; Clerk when keys set |
| `/signup` | missing | none | Mapped only |
| `/admin` | missing | none | No `admin/page.tsx`; use `/admin/dashboard` |
| `/admin/dashboard` | done | live-sample | Ledger + pulse KPI sample |
| `/admin/inventory` | done | live-sample | 27 SKUs ALP×Adam Scott |
| `/admin/inventory/audit` | partial | none | Empty scaffold headers |
| `/admin/inventory/ledgers` | done | live-sample | ~50 reps; product expand not rendered |
| `/admin/inventory/performance` | done | live-sample | Ledger-by-rep-brand |
| `/admin/inventory/refresh` | done | live-sample | 17 brands; refresh actions disabled |
| `/admin/inventory/transfers` | done | live-sample | Page 1 / ~21 |
| `/admin/inventory/transfers/terms` | pending | none | PendingLane |
| `/admin/warehouses` | done | live-full | 60 rows |
| `/admin/merchandising` | done | live-sample | Q3 41 reps; Q2 seeded; older periods disabled |
| `/admin/merchandising/stores` | pending | none | PendingLane — P0 for store grid |
| `/admin/shopify` | done | live-full | Health 9/29 + shops 29; sync optional |
| `/admin/shopify/apps` | pending | none | Live form-only; PendingLane |
| `/admin/shopify/test` | pending | none | PendingLane |
| `/admin/stores` | done | live-full | 29 brand shops |
| `/admin/stores/:id` | partial | live-sample | Detail page present; thin |
| `/admin/orders` | done | live-sample | Page 1 / ~1034 |
| `/admin/orders/drafts` | partial | none | Stub columns |
| `/admin/reports` (+ children) | partial | scaffold / proxy | Runners scaffold |
| `/admin/commissions` (+ review) | partial | scaffold | Live capture 0 rows |
| `/admin/settlements` (+ batch preview) | partial | scaffold / live-partial | Capture had settlements; twin seed still scaffold-shaped |
| `/admin/payouts` (+ rules / example) | partial | scaffold | Live redirected to rules slug |
| `/admin/payments` · `/deposits` · `/receipts` | pending | none | Cash ops |
| `/admin/pulse` | done | none | Hub |
| `/admin/pulse/signals` | done | live-sample | Adam Scott dict |
| `/admin/pulse/goals` · `/health` · `/scores` | partial | none | Consistent stubs (not PendingLane) |
| `/admin/accounts` · `/contacts` | pending | none | CRM |
| `/admin/businesses` | done | live-full | 51; detail `:id` thin |
| `/admin/locations` · `/import` | pending | none | |
| `/admin/retail-stores` | partial | derived | From assignment sample |
| `/admin/rep-assignments` | done | live-sample | 50 / 11,314; banner OK |
| `/admin/rep-assignments/relationships` | pending | none | |
| `/admin/sales-status` | pending | none | |
| `/admin/users` | done | live-full | 189; add-demo form |
| `/admin/emails` · `/files` | pending | none | |
| `/admin/diagnostics/order-timeline` | pending | none | |
| `/inventory` | partial | live-sample | Same 27-SKU sample |
| `/inventory/ledgers` · `/performance` | done | live-sample | Pages exist (rep-facing TWIN-STATUS stale) |
| `/inventory/transfers` | partial | live-sample | RO mirror |
| `/inventory/transfers/create` | partial | none | Page exists; write path limited |
| `/inventory/transfers/:id` (+ receive) | partial | live-sample | Detail/receive shells |
| `/inventory/transfers/preview` | missing | none | In URLS; no page |
| `/merchandising` | done | live-sample + optional iframe | Offline rollup + route shell; visit engine iframe |
| `/orders` · `/drafts` · `/:id` | partial | live-sample | Thin stubs |
| `/retail-stores` | partial | derived | Page exists |
| `/locations/sales-status` | partial | none | Page exists; thin |
| `/stores` | missing | none | Rep Shopify/stores counterpart |
| `/test/dashboard` | missing | none | Out of scope |

---

## 4. Data gaps

| Dataset | Seed / capture | Gap |
|---------|----------------|-----|
| Inventory levels | 27 SKUs | Need all warehouses × brands (or API dump) |
| Orders | 20 rows, page 1 of ~1034 | Paginated / async export |
| Transfers | 20 rows, page 1 of ~21 | Full queue + statuses |
| Rep assignments | 50 of 11,314 primary (~227 pages) | Full dump or filtered export API |
| Retail stores | Derived / page-1 live 50 | Dedicated `retail-stores.json` |
| Commissions | Live **0** rows; twin 14 scaffold | Re-scrape when review UI loads; replace seed |
| Settlements / payouts | Live tables existed; twin still scaffold-oriented | Re-ingest live JSON into seeds |
| Ledger | ~50-rep performance; product expand unused | Full product-line expand + charts |
| Pulse | One-rep signals | Multi-rep batch |
| Merch periods | Q2+Q3 only | Q1 / 2025 periods; visit-level Done status |
| Shopify apps | 0 list rows (form page) | N/A until apps list exists |
| Users roles | 151/189 empty Role (live DQ) | Role hygiene (live + twin filters) |
| Dashboard KPI | Capture file present | Not always wired as dedicated seed |

---

## 5. Merch-as-function gaps

Intended model (README / INTEGRATION-READY): JOEY field app is a **D2R module**, not a rival product.

| Layer | Status |
|-------|--------|
| Admin program rollup `/admin/merchandising` | **Strong offline** (Q3 columns + period switcher) |
| Rep offline rollup + Circle K route list `/merchandising` | **Strong offline shell** |
| Visit engine (GPS, photos, 7-phase survey) | Still **root CRA** + optional **iframe** via `NEXT_PUBLIC_MERCH_APP_URL` |
| `/admin/merchandising/stores` | **PendingLane** — no store grid |
| Auth | Dual login risk (Clerk/DEV shell vs merch app) |
| Embed hardening | Blank iframe if `X-Frame-Options` / CSP `frame-ancestors` blocks twin origin |
| In-app routes under `/merchandising/*` | Not migrated — iframe coexistence is transitional |

---

## 6. Webhooks / automation gaps

| Item | Twin today | Gap |
|------|------------|-----|
| `POST /api/webhooks/shopify` + HMAC | Done | Needs company host + secret rotation runbook in prod |
| Idempotent event store | `data/webhook-events/` (gitignored) | No DLQ / admin replay UI |
| Read sync `POST /api/shopify/sync` | Done (env-gated) | Single-env token ≠ live 29-shop model |
| Write / inventory adjust | **Disabled** | Blocked until ledger reconcile |
| Install + automation logs | Seed samples + runtime stubs | Live multi-shop fan-out, CRON_SECRET + Vercel cron |
| Scope health automation | Offline stubs | Must alert on Healthy→Missing / Unreachable |

Docs: `d2r-app/docs/SHOPIFY-WEBHOOKS.md` · paths in `INTEGRATION-READY.md`.

---

## 7. Deploy / security gaps

| Item | Status |
|------|--------|
| **Next.js CVE** | `package-lock` resolves **`next@15.5.7`** with npm `deprecated` → [security-update-2025-12-11](https://nextjs.org/blog/security-update-2025-12-11); peer range already allows **≥15.5.9** — bump before any public deploy |
| Clerk | Scaffold only until company publishable/secret keys |
| DEV_AUTH | Fine for offline audit; must not be production |
| Vercel / GitHub | Owner Week-0: private `Direct2Retailers/d2r-app`, team project, staging domain (`CLIENT-GO-LIVE-PLAN.md`, `OWNER-INFRA-SETUP.md`) |
| Secrets | Vault-only; no secrets on tip — keep it that way |
| Postgres | File store interim |
| Shopify Partner app | Not retargeted from CTO endpoints |

---

## 8. Recommended next 5 actions

1. **Bump Next** to a patched release (≥15.5.9 / advisory-safe) and re-run smoke + `tsc` + `next build`.
2. **Deepen cutover data:** paginated or API dumps for orders, transfers, assignments; multi-warehouse inventory matrix; re-scrape commissions when UI loads; ingest settlements/payouts live JSON into seeds.
3. **Ship company staging:** GitHub private repo + Vercel + Clerk keys; disable DEV_AUTH on that host; document webhook URL.
4. **Merch module path:** production `MERCH_APP_URL` + CSP allowlist **or** begin in-app visit routes; fill `/admin/merchandising/stores`; visit-level Done export for offline tabs.
5. **Live Shopify reliability (parallel):** scope health gate + missing-scope names + re-auth for 20 unhealthy shops — twin cannot outrun a 9/29 healthy fleet.

---

## Cross-checks

| Check | Result |
|-------|--------|
| `node scripts/qc-twin-smoke.mjs` | PASS |
| `LINK-AUDIT.md` leftover nav gaps | None for primary map; 16 stubs intentional |
| `RECONCILE-STATUS.md` | Mid-flight FAIL narrative — **superseded** by wave 3 PASS on this tip |
| Domain TWIN-STATUS | Inventory/merch/shopify/CRM strong; payouts conditional; rep-facing matrix **stale** on ledgers/create (pages now exist) |

---

## Appendix — smoke required set (all present)

Seeds (21): inventory-sample, ledger-performance, ledger-by-rep-brand, transfers, warehouses, brand-levels, businesses, merch Q2/Q3, orders, shopify-health/shops, stores, pulse-signals, users, assignments, rep-assignments, payouts, payout-rules, commissions, settlements.

Pages (23): `/`, login, admin dashboard/inventory/ledgers/transfers/refresh/warehouses/merchandising/orders/shopify/stores/pulse/signals/payouts/commissions/settlements/users/rep-assignments/reports, inventory, orders, merchandising.
